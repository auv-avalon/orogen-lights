# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(lightsTaskLib)
ADD_LIBRARY(${LIGHTS_TASKLIB_NAME} SHARED 
    ${LIGHTS_TASKLIB_SOURCES})
add_dependencies(${LIGHTS_TASKLIB_NAME}
    regen-typekit)

TARGET_LINK_LIBRARIES(${LIGHTS_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${LIGHTS_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${LIGHTS_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${LIGHTS_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${LIGHTS_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${LIGHTS_TASKLIB_HEADERS}
    DESTINATION include/orocos/lights)

